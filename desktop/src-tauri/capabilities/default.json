{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Talk to the room planner API and nothing else.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "http:default",
      "allow": [{ "url": "https://*.paul-padovani-thomas.com/*" }]
    }
  ]
}
